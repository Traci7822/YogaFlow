<%= form_for(@sequence) do |f| %>
  <h3>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </h3>
  <br />
  <br />
  <h3>
    <%= f.label :difficulty %>
  </h3>
  <h5>1-5 scale, 1 = easiest, 5 = hardest</h5>
  <%= f.select :difficulty, options_for_select(1..5) %>
  <br />
  <br />
  <%= f.fields_for :poses_attributes do |pose_builder| %>
    <% 15.times do %>
      <%= pose_builder.label :add_pose_to_this_sequence %>
      <%= pose_builder.collection_select(:pose_id, @poses, :id, :name) %><br />
    <% end %>
  <% end %>
  <br />
  <br />
  <%= f.submit %>
<% end %>
